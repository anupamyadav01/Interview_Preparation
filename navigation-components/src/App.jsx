import Navigation from "./Navigation/Navigation";
import data from "./constants/data";
const App = () => {
  return (
    <div>
      <Navigation data={data} />
    </div>
  );
};

export default App;
